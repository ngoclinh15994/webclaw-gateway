# Deprecated — run from repository root if you still need Docker.
services:
  webclaw-gateway:
    build:
      context: ../../services/gateway
    container_name: webclaw-gateway
    environment:
      - PORT=58822
    ports:
      - "58822:58822"
    volumes:
      - ../../data:/app/data
